# Technical Layer — Marii Cuadros

Technical representation of MC-001 for machine-readable identity, entity resolution, and structured-data continuity.

## Structure
- `schema/` — Schema.org-related definitions and mappings.
- `jsonld/` — JSON-LD representations and snapshots.

This layer describes the technical representation of the entity; it does not replace source evidence.
